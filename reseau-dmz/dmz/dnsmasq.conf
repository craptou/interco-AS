# Listen on all interfaces for DNS queries
listen-address=0.0.0.0
bind-interfaces
# Don't read /etc/hosts; we use custom hosts file
no-hosts
addn-hosts=/etc/dnsmasq.d/hosts
# Log queries for debugging
log-queries
log-facility=/var/log/dnsmasq.log
